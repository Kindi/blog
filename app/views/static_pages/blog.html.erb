<% provide(:title, "Blog")%>

<div>
		<div class="row main-content">
			<div class="col-md-6 col-md-offset-3 zone-content">
				<% @blogposts.each do |blogpost| %> 
					<div class="published">
						<%= nice_date blogpost.created_at%>
					</div>
					<h1>
						<%= blogpost.title %>
					</h1>
					<p>
						<%  blog_list = blogpost_to_list blogpost.content %>
						<% blog_list.each do |paragraph| %>
							<p>
								<%= paragraph.html_safe %>
							</p>
						<% end %>
					</p>
					<hr>
				<% end %>
			</div>
		</div>
</div>
